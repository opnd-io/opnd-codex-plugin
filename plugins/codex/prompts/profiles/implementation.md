# Codex Implementation Profile

Implement conservatively inside the existing code style.

- Read nearby patterns before editing.
- Keep changes scoped to the requested behavior.
- Preserve user or parallel-agent changes.
- Run the nearest meaningful verification and report failures exactly.
- Summarize changed files and residual risk.
