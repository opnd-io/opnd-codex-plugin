Our fork [`opnd-io/opnd-codex-plugin`](https://github.com/opnd-io/opnd-codex-plugin) already addresses this in the v2.0+ sprint.

## Fix location

`plugins/opnd-codex/scripts/lib/broker-lifecycle.mjs`:

- L46-48: `const BROKER_SHUTDOWN_TIMEOUT_MS = 5000`
- L65 area: `setTimeout(..., BROKER_SHUTDOWN_TIMEOUT_MS).unref()` ensures the shutdown call resolves within 5 seconds even if the broker doesn't acknowledge.

The function is structured so it **never rejects** — call sites can safely `await` it without try/catch. SessionEnd hook completes within the bounded window.

Reference: [`broker-lifecycle.mjs#L46-L65`](https://github.com/opnd-io/opnd-codex-plugin/blob/main/plugins/opnd-codex/scripts/lib/broker-lifecycle.mjs). Cross-reference [`docs/exploration/2026-05-28-upstream-issue-classification.md`](https://github.com/opnd-io/opnd-codex-plugin/blob/main/docs/exploration/2026-05-28-upstream-issue-classification.md).
